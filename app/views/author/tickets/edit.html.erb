<% provide(:page_title, "Edytuj #{@ticket.name}") %>
<% provide(:ticket_active, 'active') %>

<h1>Editing Ticket</h1>

<%= render 'form', ticket: @ticket %>

<%= link_to 'Show', @ticket %> |
<%= link_to 'Back', tickets_path %>

<% provide(:page_title, 'Wydarzenia') %>
<% provide(:author_events_active, 'active') %>


<div class="row">
  <div class="space">
    <%= link_to 'Nowe wydarzenia', new_authors_event_path, class: 'btn btn-primary' %>
  </div>
  <table class="table table-striped">
    <tr>
      <th colspan="5">
        Tytuł
      </th>
    </tr>
    <%= render @events %>
  </table>
</div>
<div class="apple_pagination">
  <%= will_paginate @events, container: false %>
</div>

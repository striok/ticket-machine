<% provide(:page_title, @post.title) %>
<% provide(:post_active, 'active') %>

<div class="row">
  <div class="col-md-1 col-lg-2"></div>
  <div class="col-md-10 col-lg-8 text-xs-center article">
    <h1><%= @post.title.upcase %></h1>
    <h6><%= @post.display_day_published %></h6>
    <span class="contents"><%= @post.body.html_safe %></span>

    <% if @post.author_id? %>
      <div class="author-bio">
        <h6>Opublikowano przez</h6>
        <%= image_tag(@post.author.gravatar_image_url) %>
        <h4><%= @post.author.name %></h4>
        <h6 class='bio'><%= @post.author.bio %></h6>
      </div>
    <% end %>

    <div id="disqus_thread"></div>
    <noscript>Please enable JavaScript to view the
      <a href="https://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>

  </div>
</div>


<script>
  var disqus_config = function () {
    this.page.url = '<%= request.original_url %>';  // Replace PAGE_URL with your page's canonical URL variable
    this.page.identifier = '<%= @post.slug %>'; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
  };

  (function () { // DON'T EDIT BELOW THIS LINE
    var d = document, s = d.createElement('script');
    s.src = 'https://sportomat.disqus.com/embed.js';
    s.setAttribute('data-timestamp', +new Date());
    (d.head || d.body).appendChild(s);
  })();
</script>


